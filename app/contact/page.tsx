import DetailPage from '@/components/DetailPage'; import {siteContent as c} from '@/content/site';
export default function Contact(){return <DetailPage eyebrow={c.contact.eyebrow} title={c.contact.title} copy={c.contact.copy}><section className="section"><div className="container detail-grid"><div><div className="kicker">Start a conversation</div><h2 className="section-title">Bring us the complexity.</h2><p className="lead">For business enquiries, reach us at <a className="gold" href={`mailto:${c.contact.email}`}>{c.contact.email}</a> or submit the form and our team will get back to you.</p><p className="lead">{c.contact.phones.join(' · ')}</p><p className="lead">{c.contact.address}</p></div><form className="contact-form"><input className="input" placeholder="Your name"/><input className="input" placeholder="Work email" type="email"/><input className="input" placeholder="Company"/><textarea className="input" placeholder="Tell us about the opportunity" rows={7}/><button className="btn btn-gold" type="submit">Send enquiry ↗</button></form></div></section></DetailPage>}
