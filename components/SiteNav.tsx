'use client';
import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {siteContent as c} from '@/content/site';
export default function SiteNav(){const p=usePathname(); const items=[['services',c.nav.services],['projects',c.nav.projects],['locations',c.nav.locations],['about',c.nav.about],['insights',c.nav.insights]] as const; return <header className="nav"><Link className="brand" href="/"><span className="brand-symbol">A</span><span className="brand-copy"><strong>{c.brand.name}</strong><small>{c.brand.sub}</small></span></Link><nav className="navlinks"><Link className={p==='/'?'active':''} href="/">{c.nav.home}</Link>{items.map(([slug,label])=><Link key={slug} className={p.startsWith('/'+slug)?'active':''} href={'/'+slug}>{label}</Link>)}</nav><Link className="navcta" href="/contact">{c.nav.cta} <span>→</span></Link><Link className="mobile-menu" href="/contact">↗</Link></header>}
